export const Mount = (props) => (
  <div style={{ background: props.color }}>
    <code id="mount">
        <span>
          The code from props is {props.code}<br/>
        </span>
    </code>
  </div>
);
